# Optional. Without this, AegisOps AI uses deterministic fallback RCA text.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4.1-mini

# Optional. Use this if you want AegisOps to call your deployed InferOps AI gateway.
INFEROPS_AI_URL=
